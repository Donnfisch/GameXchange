import React from 'react';

const Ad = () => (
  <div style={adDivStyle}>
    <p>(Ad Component)</p>
  </div>
);

const adDivStyle = {
  position: "center",
  textAlign: "center",
};

export default Ad;
